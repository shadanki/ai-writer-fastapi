SYSTEM_PROMPT = """
あなたは日本語の記事編集者兼SEOライターです。事実性・一貫性・構造化を重視し、
抽出しやすい要点箇条書き、明確な見出し階層、出典リンクの提示を行います。
ゴシップや不確かな推測は避け、固有名詞と定義を明確にします。
""".strip()

TITLE_PROMPT = """
# 目的
入力のキーワードと下書きから、読者の課題に刺さる具体的タイトルを10本。

# 制約
- 日本語、32字〜58字程度、コロンやダッシュの乱用は避ける
- 主語と対象（誰向け/何のため）を含める
- 検索意図（{intent_list}）のいずれかを明示的に満たす
- 近似タイトルは除外。多様性を確保。

# 入力
キーワード: {topic}
下書き: {draft}
推定検索意図: {inferred_intents}

# 出力フォーマット(JSON)
[
  {{"title": "...", "intent":"Informational|Transactional|Comparative", "why":"刺さる理由を1行"}}
]
""".strip()

OUTLINE_PROMPT = """
目的：{selected_title} の記事構成を作る
条件：
- 構成: 導入→H2(3-5個)→各H2にH3(2-4個)→結論(まとめ)
- 各H2の冒頭に「要点：」で3-5行の箇条書き
- 用語定義・比較表の候補を含める

出力(JSON):
{{
 "intro": "80-120字",
 "h2": [
   {{"title":"...", "h3":["...","..."], "keypoints":["...","...","..."] }}
 ],
 "outro": "100-150字"
}}
""".strip()

SECTION_PROMPT = """
対象: {h2_title}
条件：
- 先頭に「要点」の箇条書き（3-5行）
- 例・手順・チェックリスト・注意点を具体化
- 可能なら表をMarkdown表で提示
- 参照すべき一次情報(候補)を3件挙げる（URLはダミー可）

出力: 純Markdown（見出しは ## / ### を使用）
""".strip()

FINAL_POLISH_PROMPT = """
タスク：記事全体の語調/用語/冗長表現を整え、重複を削除し、事実確認の観点を挿入。
- 用語統一リスト: {glossary}
- 同義重複を削除、段落の論理接続を強化（しかし水増ししない）
- セクション間のブリッジ文を適宜1-2文追加
- 出力は完全なMarkdown本文のみ
""".strip()